import { duration } from '../../../helpers/duration';
import { module, test } from 'qunit';

module('Unit | Helper | duration');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = duration(42);
  assert.ok(result);
});
